# Aleph Manager GUI - 配置路径更新说明

## 更新内容

本次更新修改了 `aleph_managerGUI.py`，实现了以下功能：

### 1. 强制使用本地配置目录

程序现在会强制 `aleph.exe` 将所有配置文件和密钥文件保存在本地目录：

```
.\tools\.aleph-im\
  ├── config.json          # 配置文件
  └── private-keys\        # 密钥文件目录
      ├── account1.key
      ├── account2.key
      └── ...
```

**而不是**之前的用户目录：`C:\Users\{username}\.aleph-im`

### 2. 具体修改

#### 2.1 环境变量设置（第 157-161 行）

在 `run_command()` 函数中添加了环境变量设置：

```python
# 设置 Aleph 配置目录到 tools\aleph_py\.aleph-im
aleph_config_home = os.path.join(self.app_path, "tools", "aleph_py", ".aleph-im")
env["ALEPH_CONFIG_HOME"] = aleph_config_home
env["HOME"] = os.path.join(self.app_path, "tools", "aleph_py")
env["USERPROFILE"] = os.path.join(self.app_path, "tools", "aleph_py")
```

这三个环境变量确保了：
- `ALEPH_CONFIG_HOME`：Aleph SDK 优先使用的配置目录
- `HOME`：Unix 风格的主目录路径
- `USERPROFILE`：Windows 风格的用户配置文件路径

#### 2.2 移除 WSL 依赖

**删除的功能**：
- `get_wsl_username()` 函数（不再需要）
- 所有与 WSL 相关的路径处理

**更新的路径**：
- 切换账户时的密钥路径（第 424 行）
- 删除账户时的文件路径（第 489 行）

从 WSL 路径：
```python
key_path = f"/home/{wsl_username}/.aleph-im/private-keys/{account_name}.key"
```

改为 Windows 路径：
```python
key_path = os.path.join(self.app_path, "tools", "aleph_py", ".aleph-im", "private-keys", f"{account_name}.key")
```

#### 2.3 配置目录管理（第 525-580 行）

新增两个函数：

**`ensure_config_directory()`**：
- 确保配置目录和密钥目录存在
- 如果不存在则自动创建
- 在程序初始化时调用（第 125 行）

**`migrate_old_config()`**：
- 自动检测用户目录下的旧配置（`C:\Users\{username}\.aleph-im`）
- 如果存在，自动将配置文件和密钥文件迁移到新位置
- 保证用户不会丢失已有的账户

#### 2.4 改进的删除账户功能（第 468-522 行）

使用标准的 Windows 文件操作：
- 使用 `os.path.exists()` 检查文件是否存在
- 使用 `os.remove()` 删除文件
- 提供更好的权限错误提示

### 3. 使用方式

#### 3.1 首次运行

程序会自动：
1. 创建 `.\tools\aleph_py\.aleph-im` 目录
2. 检查用户目录下是否有旧配置
3. 如果有，自动迁移到新位置
4. 所有后续操作都在新位置进行

#### 3.2 创建账户

创建的账户密钥文件将自动保存到：
```
.\tools\aleph_py\.aleph-im\private-keys\{账户名}.key
```

#### 3.3 检查配置位置

在操作日志中可以看到：
```
配置目录已确认: C:\...\tools\aleph_py\.aleph-im
```

### 4. 优势

1. **便携性**：所有配置在程序目录内，可以整体移动
2. **独立性**：不依赖用户目录，避免多用户冲突
3. **透明性**：配置文件位置明确，易于查找和备份
4. **兼容性**：自动迁移旧配置，无需手动操作

### 5. 注意事项

#### 5.1 目录权限

确保程序对 `.\tools\aleph_py\.aleph-im` 目录有读写权限。如果遇到权限问题：
- 以管理员身份运行程序
- 或者修改目录权限

#### 5.2 备份

建议定期备份 `.\tools\aleph_py\.aleph-im\private-keys` 目录，因为密钥文件丢失将无法恢复账户。

#### 5.3 环境变量

程序会在每次调用 `aleph_py.exe` 时设置环境变量，这些设置仅对当前进程有效，不会影响系统全局环境。

### 6. 故障排除

#### 问题：程序仍然从用户目录读取配置

**解决方案**：
1. 检查操作日志中的 "配置目录已确认" 消息
2. 确认环境变量设置正确
3. 删除用户目录下的旧配置文件以强制使用新位置

#### 问题：找不到账户

**解决方案**：
1. 检查 `.\tools\aleph_py\.aleph-im\private-keys` 目录
2. 确认密钥文件（.key）存在
3. 尝试重启程序，让迁移功能执行

#### 问题：删除账户失败

**解决方案**：
1. 检查文件权限
2. 确保文件没有被其他程序占用
3. 尝试以管理员身份运行

### 7. 技术细节

#### 环境变量优先级

`aleph_py.exe` 按以下顺序确定配置目录：

1. `ALEPH_CONFIG_HOME`（如果设置）✅ **我们设置了这个**
2. `XDG_DATA_HOME/.aleph-im`（如果设置）
3. `~/.aleph-im`（默认，使用 HOME 或 USERPROFILE）✅ **我们也覆盖了这个**

#### 路径规范化

所有路径使用 `os.path.join()` 生成，确保跨平台兼容性。

---

**版本**：v1.1.0+
**更新日期**：2025-11-16
**作者**：层林尽染
